<tr>
  <td><%= service.description %></td>
  <td><img src="<%= service.imagem.url(:thumb) %>" alt=""></td>
  <td>R$ <%= service.price %></td>
  <td>
    <%= button_to "Remover Serviço", service, method: :delete, class: 'btn btn-danger',
                  data: {confirm: "Tem certeza que deseja remover #{service.description} ?"}%>
  </td>
  <td>
    <%= link_to "Alterar", edit_service_path(service), class: 'btn btn-primary' %>
  </td>
</tr>